pkgname=nexora-bin
_pkgname=nexora
pkgver=x.y.z
pkgrel=1
pkgdesc="A lightweight, developer-focused database management tool"
arch=('x86_64')
license=('Apache-2.0')
provides=("${_pkgname}")
conflicts=("${_pkgname}")
depends=('webkit2gtk-4.1' 'gtk3' 'libappindicator-gtk3' 'openssl')
source=("https://github.com/privixy/nexora/releases/download/v${pkgver}/Nexora_${pkgver}_amd64.deb")
sha256sums=('SKIP')

package() {
    bsdtar -xf data.tar.* -C "${pkgdir}"
}
