# Nexora UI Extension

This folder contains the React UI extension for the parent Nexora plugin.

`dist/index.js` is a single IIFE bundle that the Nexora host evaluates at runtime.

Types for slot contexts, hooks and helpers come from `@nexora/plugin-api`.
