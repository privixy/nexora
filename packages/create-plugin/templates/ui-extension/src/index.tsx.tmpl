// UI extension for the ${DISPLAY_NAME} plugin.
//
// This bundle contributes a component to the `data-grid.toolbar.actions`
// slot — you'll see the button in the top-right of any data grid view
// when the active connection uses this driver.

import { defineSlot, usePluginToast } from "@nexora/plugin-api";

const Toolbar = defineSlot("data-grid.toolbar.actions", ({ context }) => {
  const { showInfo } = usePluginToast();

  const handleClick = () => {
    showInfo(
      "Hello from ${DISPLAY_NAME}! Active table: " + (context.tableName ?? "(none)"),
    );
  };

  return (
    <button
      type="button"
      onClick={handleClick}
      style={{
        padding: "4px 10px",
        fontSize: "12px",
        border: "1px solid currentColor",
        borderRadius: "4px",
        background: "transparent",
        color: "inherit",
        cursor: "pointer",
      }}
    >
      ${DISPLAY_NAME}
    </button>
  );
});

export default Toolbar.component;
